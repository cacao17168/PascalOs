# Main
PascalOs is a test project. The binary just simulates operating system work.
## Dependencies
Make sure that you are working on linux and you've installed fpc:
```sh
sudo apt install/pacman -S fpc
```
